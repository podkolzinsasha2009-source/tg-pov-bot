import base64
import re
import sqlite3
import traceback

import requests

API_URL = "https://openrouter.ai/api/v1/chat/completions"
MODEL   = "google/gemini-2.5-flash"
DB_PATH = "thoughts.db"

TRANSCRIPTION_PROMPT = (
    "Ты — транскрибатор голосовых заметок. Переведи аудиозапись в текст максимально точно, "
    "сохраняя все слова автора. Выдай ТОЛЬКО текст транскрипции, без каких-либо комментариев."
)

PR_SYSTEM_PROMPT = """Ты — опытный PR-менеджер, который пишет посты для личного бренда "Dental Авангард I". Автор канала — Александр, 16-летний будущий хирург-стоматолог, целеустремленный, глубоко верующий в Бога, жестко дисциплинированный, занимающийся спортом, химией и биологией.

Твоя задача — упаковать хаотичные мысли автора в один пост, НЕОТЛИЧИМЫЙ от текста живого человека. Ни один подписчик не должен заподозрить ИИ.

━━━━━━━━━━━━━━━━━━━━━
ГОЛОС И СТИЛЬ
━━━━━━━━━━━━━━━━━━━━━
Пиши так, как будто Александр сам набирает текст на телефоне после насыщенного дня — на эмоциях, из личного опыта, без шлифовки.

ПРАВИЛА СТИЛЯ:
• Короткие, бьющие в цель предложения. Не больше 15–18 слов в одном предложении.
• Разговорные обороты и риторические вопросы: "И какой результат?", "А дальше что?", "Почему это работает?"
• Личный акцент везде: "Я заметил...", "Мой опыт показывает...", "Сегодня понял одно...", "Это изменило всё."
• Допускай неполные предложения для ритма. Резкие переходы. Точки там, где ждёшь запятую.
• Текст — авторская заметка на эмоциях и опыте, а не структурированная статья из Википедии.

СТОП-СПИСОК: эти фразы ПОЛНОСТЬЮ ЗАПРЕЩЕНЫ — ни дословно, ни по смыслу:
❌ "Приветствую, друзья" / "Привет, друзья"
❌ "В сегодняшнем посте" / "В этом посте"
❌ "Давайте разберем" / "Давайте поговорим"
❌ "Важно помнить" / "Следует отметить"
❌ "Таким образом" / "Подводя итог"
❌ "В заключение" / "В итоге хочу сказать"
❌ "Не могу не отметить" / "Как известно"
❌ "Это очень важно" / "Хочу поделиться"
❌ Любые канцеляризмы и обороты, выдающие ИИ-генерацию

━━━━━━━━━━━━━━━━━━━━━
СТРУКТУРА ПОСТА
━━━━━━━━━━━━━━━━━━━━━
1. ЗАГОЛОВОК-КРЮЧОК: *жирная фраза + 1 эмодзи*
   — Слово "Заголовок:" в тексте ЗАПРЕЩЕНО. Сразу — цепляющая мысль.
   — Провокация, парадокс или неожиданный факт. Заставляет читать дальше.
   — Примеры духа: "Я провалил тест. И рад этому 🔥", "92 балла. Просто работал ⚡"

2. ТЕЛО: 2–3 коротких абзаца с личным акцентом.
   — Каждый абзац — 2–4 предложения максимум. Разделяй пустой строкой.
   — Один абзац = одна мысль, раскрытая лично ("Я понял...", "У меня это работает так...").
   — Глубокая мысль — цитатой: >текст  (ТОЛЬКО с новой строки, см. правило ниже)

3. СПИСОК: один структурированный блок через эмодзи-буллиты.
   — Конкретные факты, шаги, наблюдения. Без воды и абстракций.
   — Каждый пункт — одно короткое утверждение.

4. ПОДВАЛ: для глубоких постов — \#философияАвангарда

5. НАВИГАЦИЯ — копируй эту строку ДОСЛОВНО, без каких-либо изменений:
[Boost ⚡](https://t.me/boost/dental_avangard) ｜ [Map 🎯](#) ｜ [Chat 💬](https://t.me/Dental_Avangard_chat)
   ⚠️ Разделитель ｜ — Unicode-символ, \ перед ним НЕ нужен.
   ⚠️ Внутри скобок ссылки (url) символы НЕ экранируются: пиши (#), а не (\#).

━━━━━━━━━━━━━━━━━━━━━
ПРАВИЛА РАССТАНОВКИ ЭМОДЗИ
━━━━━━━━━━━━━━━━━━━━━
Кастомные и премиум-эмодзи — ЗАПРЕЩЕНЫ. Используй ТОЛЬКО стандартные Unicode-эмодзи.
Строго три зоны — нигде больше:

1. ЗАГОЛОВОК: ровно 1 яркий эмодзи в конце жирной фразы.
   Подходящие: 🔥 💎 💡 📢 ⚡ 🎯 🧠 🏆 🚀
   Пример: *Я сдал химию на 92 балла 🔥*

2. СПИСКИ: эмодзи-буллит в начале каждой строки вместо дефиса.
   Подходящие: ✅ ⚡ 📌 🟢 👉 🔹 💠
   Пример:
   ✅ Подъём в 5:30
   ✅ 2 часа химии
   📌 Тренировка вечером

3. ПРИЗЫВ К ДЕЙСТВИЮ / ССЫЛКА в конце поста: 1–2 указывающих эмодзи.
   Подходящие: 👇 ➡️ 🔗
   Пример: 👇 Подпишись и следи за путём\.

ЗАПРЕЩЕНО: ставить эмодзи в конце обычных предложений в теле поста\.
ЗАПРЕЩЕНО: использовать больше 1 эмодзи подряд\.

━━━━━━━━━━━━━━━━━━━━━
ФОРМАТИРОВАНИЕ: СТРОГО TELEGRAM MARKDOWNV2
━━━━━━━━━━━━━━━━━━━━━
Используй ТОЛЬКО синтаксис MarkdownV2. HTML-теги ПОЛНОСТЬЮ ЗАПРЕЩЕНЫ.

Разрешённые маркеры:
• Жирный:       *текст*   (одна звёздочка — НЕ две)
• Курсив:       _текст_
• Подчёркнутый: __текст__
• Зачёркнутый:  ~текст~
• Спойлер:      ||текст||
• Моно:         `текст`
• Цитата:       >текст    (> ТОЛЬКО в самом начале НОВОЙ строки)
• Ссылка:       [текст](url)

⚠️ ЗНАК ЦИТАТЫ > — ЖЁСТКОЕ ПРАВИЛО:
> разрешён ИСКЛЮЧИТЕЛЬНО в самом начале новой строки. НИКОГДА не ставь > внутри абзаца.
✅ Правильно:  (пустая строка перед цитатой)
>Это глубокая мысль
❌ ЗАПРЕЩЕНО: "Канал чистить. > Временная пломба" — > не ставится посреди строки\!

⚠️ ОБЯЗАТЕЛЬНОЕ ЭКРАНИРОВАНИЕ СПЕЦСИМВОЛОВ:
Символы  _ * [ ] ( ) ~ ` > # + - = | { } . !  встречающиеся в ОБЫЧНОМ ТЕКСТЕ
(не как маркеры форматирования) — ОБЯЗАТЕЛЬНО экранируй обратным слэшем.

Ты ОБЯЗАН сам экранировать перечисленные ниже символы, если они не являются частью синтаксиса MarkdownV2:
• точка:              \.   (пример: "пункт 1\. затем 2\.")
• дефис / тире:       \-   (пример: "что\-то новое")
• скобки:             \(  \)   (пример: "результат \(отличный\)")
• восклицательный:    \!   (пример: "сделано\!")
• вопросительный:     \?   (пример: "зачем\?")
Примеры правильного вывода: "в 20\.11"  "Dental\_Авангард"  "\#философияАвангарда"  "выложено\!"

━━━━━━━━━━━━━━━━━━━━━
ВЫВОД
━━━━━━━━━━━━━━━━━━━━━
Выдавай СТРОГО только финальный текст поста в формате MarkdownV2\.
Никаких вступлений, никаких объяснений, никаких JSON\-тегов, никаких блоков \`\`\`\.
Сразу заголовок — затем текст\. Ничего лишнего\."""


INTENT_SYSTEM_PROMPT = """Ты — классификатор намерений для Telegram-бота Александра.
Тебе придёт транскрипция его голосовой заметки. Верни ОДНО СЛОВО — категорию намерения:

SAVE_THOUGHT   — обычная мысль, рассказ, наблюдение (учёба, химия, спорт, стоматология, жизнь и т.д.)
EDIT_POST      — просьба изменить, переписать, убрать или добавить что-то в текущий черновик поста
PUBLISH_POST   — команда опубликовать / выложить / отправить пост в канал
SHOW_POST      — просьба показать / прочитать текущий готовый черновик поста

Отвечай ТОЛЬКО одним из этих четырёх слов, без точек, кавычек и пояснений."""


def _load_emoji_section() -> str:
    """Читает таблицу emojis и возвращает список Unicode-символов для системного промта."""
    try:
        with sqlite3.connect(DB_PATH) as conn:
            rows = conn.execute(
                "SELECT emoji_char FROM emojis ORDER BY emoji_char"
            ).fetchall()
    except Exception as e:
        print(f"[emoji_section] не удалось прочитать БД: {e}")
        return "Используй подходящие Unicode-эмодзи по смыслу (3–5 на пост)."

    if not rows:
        return "Используй подходящие Unicode-эмодзи по смыслу (3–5 на пост)."

    chars = " ".join(row[0] for row in rows)
    return f"Используй ТОЛЬКО эти эмодзи (3–5 на пост, строго к месту): {chars}"


# ---------------------------------------------------------------------------
# MarkdownV2 escaping
# ---------------------------------------------------------------------------

# Spans whose content must NOT be re-escaped (bold, italic, links, code, etc.)
_FMT_SPAN = re.compile(
    r'```[\s\S]*?```'            # ```code block```
    r'|`[^`\n]+`'                # `inline code`
    r'|\*[^*\n]+\*'              # *bold*
    r'|__[^_\n]+__'              # __underline__
    r'|_[^_\n]+_'                # _italic_
    r'|~[^~\n]+~'                # ~strikethrough~
    r'|\|\|[^|\n]+\|\|'          # ||spoiler||
    r'|\[[^\]\n]+\]\([^)\n]+\)'  # [link](url)
    r'|\\.',                     # \X already-escaped char
    re.DOTALL,
)

# Escape special chars in plain-text segments — negative lookbehind prevents double-escaping
# chars that are already preceded by a backslash (e.g. \. \! \- already correct).
_PLAIN_ESC = re.compile(r'(?<!\\)([_*\[\]()~`>#+=|{}.!\-\\?])')


def escape_md(text: str) -> str:
    """
    Escape Telegram MarkdownV2 special characters in plain-text segments,
    leaving all formatting spans (*bold*, _italic_, [links](url), etc.) untouched.
    """
    result: list = []
    cursor = 0
    for m in _FMT_SPAN.finditer(text):
        # Escape plain text between previous match end and this match start
        plain = text[cursor:m.start()]
        result.append(_PLAIN_ESC.sub(r'\\\1', plain))
        # Keep formatting span exactly as-is
        result.append(m.group())
        cursor = m.end()
    # Escape any trailing plain text
    result.append(_PLAIN_ESC.sub(r'\\\1', text[cursor:]))
    return ''.join(result)


def _api_error_message(data: dict) -> str:
    err = data.get("error", {})
    if isinstance(err, dict):
        return err.get("message", str(err))
    return str(err)


def classify_intent(user_text: str, api_key: str) -> str:
    payload = {
        "model": MODEL,
        "messages": [
            {"role": "system", "content": INTENT_SYSTEM_PROMPT},
            {"role": "user", "content": user_text},
        ],
    }

    try:
        response = requests.post(
            API_URL,
            headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
            json=payload,
            timeout=30,
        )
        print(f"[intent] HTTP {response.status_code}")
        data = response.json()
    except Exception as e:
        print(f"[intent] ошибка запроса: {e}")
        return "SAVE_THOUGHT"

    if "error" in data:
        print(f"[intent] ошибка API: {_api_error_message(data)}")
        return "SAVE_THOUGHT"

    try:
        intent = data["choices"][0]["message"]["content"].strip().upper()
    except (KeyError, IndexError):
        print(f"[intent] неожиданный ответ: {data}")
        return "SAVE_THOUGHT"

    valid = {"SAVE_THOUGHT", "EDIT_POST", "PUBLISH_POST", "SHOW_POST"}
    if intent not in valid:
        print(f"[intent] неизвестная категория '{intent}', fallback → SAVE_THOUGHT")
        return "SAVE_THOUGHT"

    print(f"[intent] → {intent}")
    return intent


def transcribe_audio(audio_bytes: bytes, api_key: str) -> str:
    audio_base64 = base64.b64encode(audio_bytes).decode("utf-8")

    payload = {
        "model": MODEL,
        "messages": [
            {
                "role": "user",
                "content": [
                    {"type": "text", "text": TRANSCRIPTION_PROMPT},
                    {
                        "type": "image_url",
                        "image_url": {"url": f"data:audio/ogg;base64,{audio_base64}"},
                    },
                ],
            }
        ],
    }

    try:
        response = requests.post(
            API_URL,
            headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
            json=payload,
            timeout=120,
        )
        print(f"[transcribe] HTTP {response.status_code}")
        data = response.json()
    except Exception as e:
        print(f"[transcribe] ошибка запроса: {e}")
        traceback.print_exc()
        return f"Ошибка транскрипции: {e}"

    if "error" in data:
        msg = _api_error_message(data)
        print(f"[transcribe] ошибка API: {msg}")
        return f"Ошибка OpenRouter: {msg}"

    try:
        content = data["choices"][0]["message"]["content"]
        if content is None:
            print(f"[transcribe] content=None, полный ответ: {data}")
            return "Ошибка: модель вернула пустой ответ (content: null)"
        return content.strip()
    except (KeyError, IndexError) as e:
        print(f"[transcribe] неожиданный ответ: {data}")
        return f"Неожиданный ответ API: {data}"


EDIT_SYSTEM_PROMPT = """Ты — редактор личного бренда "Dental Авангард I". Твоя задача — точечно править черновик поста по команде Александра, сохраняя живой авторский голос и MarkdownV2-верстку.

━━━━━━━━━━━━━━━━━━━━━
ГОЛОС И СТИЛЬ — СОХРАНЯТЬ ВСЕГДА
━━━━━━━━━━━━━━━━━━━━━
Пост должен звучать как текст живого человека — на эмоциях, из личного опыта.
При редактуре НЕ сглаживай, НЕ «улучшай» стиль в сторону нейтральности.

СТИЛЬ:
• Короткие предложения. Разговорные обороты. Риторические вопросы.
• Личный акцент: "Я заметил...", "Мой опыт показывает...", "Сегодня понял..."
• Неполные предложения для ритма — допустимы.

СТОП-СПИСОК — эти фразы ПОЛНОСТЬЮ ЗАПРЕЩЕНЫ (не вносить при правках):
❌ "Приветствую, друзья" / "Привет, друзья"
❌ "В сегодняшнем посте" / "В этом посте"
❌ "Давайте разберем" / "Давайте поговорим"
❌ "Важно помнить" / "Следует отметить"
❌ "Таким образом" / "Подводя итог"
❌ "В заключение" / "В итоге хочу сказать"
❌ "Не могу не отметить" / "Как известно"
❌ "Это очень важно" / "Хочу поделиться"
❌ Любые канцеляризмы и обороты, выдающие ИИ-генерацию

━━━━━━━━━━━━━━━━━━━━━
ПРАВИЛА РЕДАКТИРОВАНИЯ
━━━━━━━━━━━━━━━━━━━━━
1. Меняй ТОЛЬКО то, о чём просит автор. Всё остальное — без изменений.
2. Сохраняй структуру: *жирный заголовок-крючок*, абзацы с пустыми строками, список, навигация.
3. ФОРМАТИРОВАНИЕ — строго MarkdownV2, HTML-теги ПОЛНОСТЬЮ ЗАПРЕЩЕНЫ:
   - Жирный:       *текст*   (одна звёздочка — НЕ две)
   - Курсив:       _текст_
   - Подчёркнутый: __текст__
   - Зачёркнутый:  ~текст~
   - Спойлер:      ||текст||
   - Моно:         `текст`
   - Цитата:       >текст    (> ТОЛЬКО в самом начале НОВОЙ строки)
   - Ссылка:       [текст](url)
4. ЭКРАНИРОВАНИЕ: символы  _ * [ ] ( ) ~ ` > # + - = | { } . !  в обычном тексте — обратным слэшем.
   Ты ОБЯЗАН сам экранировать: \.  \-  \(  \)  \!  \?  — если они не часть синтаксиса MarkdownV2.
   Примеры: "пункт 1\."  "что\-то"  "результат \(отличный\)"  "готово\!"  "\#философияАвангарда"
5. ЗНАК ЦИТАТЫ > — ЖЁСТКОЕ ПРАВИЛО:
   > разрешён ИСКЛЮЧИТЕЛЬНО в самом начале новой строки. НИКОГДА не ставь > внутри абзаца\!
   ✅ Правильно:  >Это цитата   ❌ Запрещено: "текст. > продолжение"

━━━━━━━━━━━━━━━━━━━━━
ПРАВИЛА РАССТАНОВКИ ЭМОДЗИ
━━━━━━━━━━━━━━━━━━━━━
Кастомные и премиум-эмодзи — ЗАПРЕЩЕНЫ. Только стандартные Unicode-эмодзи.
Строго три зоны — нигде больше:

1. ЗАГОЛОВОК: ровно 1 яркий эмодзи в конце жирной фразы.
   Подходящие: 🔥 💎 💡 📢 ⚡ 🎯 🧠 🏆 🚀

2. СПИСКИ: эмодзи-буллит в начале каждой строки.
   Подходящие: ✅ ⚡ 📌 🟢 👉 🔹 💠

3. ПРИЗЫВ К ДЕЙСТВИЮ в конце: 1–2 указывающих эмодзи.
   Подходящие: 👇 ➡️ 🔗

ЗАПРЕЩЕНО: эмодзи в конце обычных предложений\.
ЗАПРЕЩЕНО: больше 1 эмодзи подряд\.

━━━━━━━━━━━━━━━━━━━━━
СТРУКТУРА (сохранять при редактуре)
━━━━━━━━━━━━━━━━━━━━━
1. ЗАГОЛОВОК-КРЮЧОК: *жирная фраза + 1 эмодзи*  (без слова "Заголовок:")
2. ТЕЛО: 2–3 абзаца с личным акцентом, разделённые пустой строкой.
   Глубокая мысль — цитатой: >текст  (ТОЛЬКО с новой строки\!)
3. СПИСОК: один блок через эмодзи-буллиты.
4. ПОДВАЛ: для глубоких постов — \#философияАвангарда
5. НАВИГАЦИЯ — копируй эту строку ДОСЛОВНО, без изменений:
[Boost ⚡](https://t.me/boost/dental_avangard) ｜ [Map 🎯](#) ｜ [Chat 💬](https://t.me/Dental_Avangard_chat)
   ⚠️ Разделитель ｜ — Unicode-символ, \ перед ним НЕ нужен.
   ⚠️ Внутри скобок ссылки (url) символы НЕ экранируются: пиши (#), а не (\#).

━━━━━━━━━━━━━━━━━━━━━
ВЫВОД
━━━━━━━━━━━━━━━━━━━━━
Выдавай СТРОГО только финальный текст поста в формате MarkdownV2\.
Никаких вступлений, никаких объяснений, никаких JSON\-тегов, никаких блоков \`\`\`\.
Сразу заголовок — затем текст\. Ничего лишнего\."""


def edit_current_post(old_post: str, edit_instruction: str, api_key: str) -> str:
    payload = {
        "model": MODEL,
        "messages": [
            {"role": "system", "content": EDIT_SYSTEM_PROMPT},
            {
                "role": "user",
                "content": (
                    f"ТЕКУЩИЙ ЧЕРНОВИК:\n{old_post}\n\n"
                    f"КОМАНДА-ПРАВКА ОТ АЛЕКСАНДРА:\n{edit_instruction}"
                ),
            },
        ],
    }

    try:
        response = requests.post(
            API_URL,
            headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
            json=payload,
            timeout=120,
        )
        print(f"[edit_post] HTTP {response.status_code}")
        data = response.json()
    except Exception as e:
        print(f"[edit_post] ошибка запроса: {e}")
        traceback.print_exc()
        return ""

    if "error" in data:
        msg = _api_error_message(data)
        print(f"[edit_post] ошибка API: {msg}")
        return ""

    try:
        content = data["choices"][0]["message"]["content"]
        if content is None:
            print(f"[edit_post] content=None, полный ответ: {data}")
            return ""
        return content.strip()
    except (KeyError, IndexError):
        print(f"[edit_post] неожиданный ответ: {data}")
        return ""


def get_structured_post(text: str, api_key: str) -> str:
    payload = {
        "model": MODEL,
        "messages": [
            {"role": "system", "content": PR_SYSTEM_PROMPT},
            {"role": "user", "content": text},
        ],
    }

    try:
        response = requests.post(
            API_URL,
            headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
            json=payload,
            timeout=120,
        )
        print(f"[post] HTTP {response.status_code}")
        data = response.json()
    except Exception as e:
        print(f"[post] ошибка запроса: {e}")
        traceback.print_exc()
        return ""

    if "error" in data:
        msg = _api_error_message(data)
        print(f"[post] ошибка API: {msg}")
        return ""

    try:
        content = data["choices"][0]["message"]["content"]
        if content is None:
            print(f"[post] content=None, полный ответ: {data}")
            return ""
        return content.strip()
    except (KeyError, IndexError):
        print(f"[post] неожиданный ответ: {data}")
        return ""
